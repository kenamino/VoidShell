# VoidShell .npmrc
# Ensures node-pty and other native modules are compiled for Electron,
# not the system Node.js, on all platforms.

# Use Electron's headers when building native modules
# (electron-rebuild reads ELECTRON_VERSION from package.json automatically)

# Disable optional dependencies that may fail on CI
optional=false
